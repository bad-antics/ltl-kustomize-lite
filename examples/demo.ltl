// examples/demo.ltl — runnable demo for ltl_kustomize_lite

import std::io
use ltl_kustomize_lite as lib

fn main() {
    io::println("ltl_kustomize_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}